<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.camunda.connector</groupId>
    <artifactId>connector-aws-parent</artifactId>
    <version>8.10.0-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <name>connector-aws-comprehend</name>
  <description>Camunda AWS Comprehend Connector</description>
  <artifactId>connector-aws-comprehend</artifactId>
  <packaging>jar</packaging>

  <licenses>
    <license>
      <name>Camunda Self-Managed Free Edition license</name>
      <url>https://camunda.com/legal/terms/cloud-terms-and-conditions/camunda-cloud-self-managed-free-edition-terms/</url>
    </license>
    <license>
      <name>Camunda Self-Managed Enterprise Edition license</name>
    </license>
  </licenses>

  <properties>
    <license.inlineheader>Copyright Camunda Services GmbH and/or licensed to Camunda Services GmbH
      under one or more contributor license agreements. Licensed under a proprietary license.
      See the License.txt file for more information. You may not use this file
      except in compliance with the proprietary license.</license.inlineheader>
  </properties>

  <dependencies>
    <dependency>
      <groupId>io.camunda.connector</groupId>
      <artifactId>connector-aws-base</artifactId>
      <version>${project.version}</version>
    </dependency>

    <dependency>
      <groupId>software.amazon.awssdk</groupId>
      <artifactId>comprehend</artifactId>
    </dependency>
    <dependency>
      <groupId>software.amazon.awssdk</groupId>
      <artifactId>aws-core</artifactId>
      <version>${version.aws-sdk2}</version>
    </dependency>
    <dependency>
      <groupId>software.amazon.awssdk</groupId>
      <artifactId>http-client-spi</artifactId>
      <version>${version.aws-sdk2}</version>
    </dependency>

    <!-- Required at compile scope: the v2 client/response types (e.g. ComprehendClient,
    ClassifyDocumentResponse) implement supertypes declared in sdk-core (SdkClient, SdkResponse,
    SdkPojo, SdkRequest, ...). javac must resolve those supertypes even though this module's code
    never imports them by name, so sdk-core cannot be narrowed to test scope here; matching how
    aws-eventbridge, aws-s3 and aws-lambda declare it. -->
    <dependency>
      <groupId>software.amazon.awssdk</groupId>
      <artifactId>sdk-core</artifactId>
    </dependency>

    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-lang3</artifactId>
    </dependency>

    <!-- only used by golden-JSON shape tests: no production code in this module uses Jackson
    directly, so the compile-scope dependency inherited from the root connectors POM must be
    narrowed to test scope here. -->
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <scope>test</scope>
    </dependency>

  </dependencies>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-dependency-plugin</artifactId>
        <configuration>
          <!-- sdk-core is required at compile scope for javac to resolve the v2 client/response
          supertypes (SdkClient, SdkResponse, SdkPojo, SdkRequest, ...), but bytecode-level
          dependency analysis only sees it referenced from test classes, so it is otherwise
          flagged as a "non-test scoped test only dependency". -->
          <ignoredNonTestScopedDependencies combine.children="append">
            <dependency>software.amazon.awssdk:sdk-core</dependency>
          </ignoredNonTestScopedDependencies>
        </configuration>
      </plugin>
      <plugin>
        <groupId>io.camunda.connector</groupId>
        <artifactId>element-template-generator-maven-plugin</artifactId>
        <version>${project.version}</version>
        <configuration>
          <connectors>
            <connector>
              <connectorClass>io.camunda.connector.comprehend.ComprehendConnectorFunction</connectorClass>
              <files>
                <file>
                  <templateId>io.camunda.connectors.AWSCOMPREHEND.v1</templateId>
                  <templateFileName>aws-comprehend-outbound-connector.json</templateFileName>
                </file>
              </files>
              <generateHybridTemplates>true</generateHybridTemplates>
            </connector>
          </connectors>
          <versionHistoryEnabled>true</versionHistoryEnabled>
          <includeDependencies>
            <includeDependency>io.camunda.connector:connector-aws-base</includeDependency>
          </includeDependencies>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>


