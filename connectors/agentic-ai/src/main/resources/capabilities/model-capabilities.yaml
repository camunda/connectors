# Model capability matrix for the agentic-ai connector.
#
# This file is loaded as a Spring Boot PropertySource at startup (lowest
# precedence) by `CapabilityMatrixEnvironmentPostProcessor`. Library consumers
# can override or extend any value via `application.yml` using the same
# property prefix, no full-file replacement required:
#
#   camunda.connector.agenticai.aiagent.framework.capabilities:
#     anthropic-messages:
#       models:
#         claude-opus-4-7:                # map key reused -> override existing entry
#           capabilities:
#             max-output-tokens: 64000    # only this field overridden
#
# Structure under `capabilities`:
#   <api-family>:
#     defaults:    capability block applied to every model entry in the family
#     models:      map of opaque identifiers to entries. Each entry has one of:
#                    - `id`     (defaults to the map key when neither is set)
#                    - `pattern` (glob using `*` only; longest match wins at
#                                 resolve-time)
#                  plus an optional `aliases` list (id entries only) and a
#                  `capabilities` overlay.
#                  NB: `*` and `.` in property keys interfere with Spring Boot's
#                  map binding, so glob characters always live in the
#                  `pattern` field, never in the map key.
#
# Merge semantics (Spring Boot config + ADR-005 capability matrix):
#   * Maps merge recursively (sub-keys of input-modalities / output-modalities
#     are inherited individually)
#   * Lists replace wholesale
#   * Scalars and booleans replace
#
# Resolution chain (per ADR-005 §"Resolution order"):
#   1. Connector config override (per-call, future)
#   2. Exact id or alias match
#   3. Pattern (longest-match wins)
#   4. Conservative defaults (text-only, all flags false)
#
# Modality vocabulary: text | image | pdf | audio | video. Modality lists per
# location (user-message, tool-result, assistant-message) are symmetric.

camunda:
  connector:
    agenticai:
      aiagent:
        framework:
          capabilities:

            anthropic-messages:
              defaults:
                input-modalities:
                  user-message: [text, image, pdf]
                  tool-result:  [text, image, pdf]
                output-modalities:
                  assistant-message: [text]
                supports-reasoning: false
                supports-reasoning-signature-roundtrip: false
                supports-prompt-caching: true
                supports-parallel-tool-calls: true
                context-window: 200000
                max-output-tokens: 8192
              models:
                claude-opus-4:
                  pattern: claude-opus-4-*
                  capabilities:
                    supports-reasoning: true
                    supports-reasoning-signature-roundtrip: true
                    max-output-tokens: 32000
                claude-sonnet-4:
                  pattern: claude-sonnet-4-*
                  capabilities:
                    supports-reasoning: true
                    supports-reasoning-signature-roundtrip: true
                    max-output-tokens: 64000
                claude-haiku-4:
                  pattern: claude-haiku-4-*
                  capabilities:
                    input-modalities:
                      # tool-result inherited from defaults
                      user-message: [text, image]
                    max-output-tokens: 16000
                claude-3-7-sonnet:
                  pattern: claude-3-7-sonnet-*
                  capabilities:
                    supports-reasoning: true
                    supports-reasoning-signature-roundtrip: true
                    max-output-tokens: 64000
                claude-3-5-sonnet:
                  pattern: claude-3-5-sonnet-*
                  capabilities: {}
                claude-3-5-haiku:
                  pattern: claude-3-5-haiku-*
                  capabilities:
                    input-modalities:
                      user-message: [text]
                      tool-result:  [text]
                claude-fallback:
                  pattern: claude-*
                  capabilities: {}

            # Chat Completions tool messages are text-only (the SDK enforces
            # ChatCompletionContentPartText for tool message content arrays);
            # multimodal tool results require the Responses API.
            openai-completions:
              defaults:
                input-modalities:
                  user-message: [text, image]
                  tool-result:  [text]
                output-modalities:
                  assistant-message: [text]
                supports-reasoning: false
                supports-reasoning-signature-roundtrip: false
                supports-prompt-caching: true
                supports-parallel-tool-calls: true
                context-window: 128000
                max-output-tokens: 4096
              models:
                gpt-5:
                  pattern: gpt-5*
                  capabilities:
                    supports-reasoning: true
                    context-window: 400000
                    max-output-tokens: 128000
                o1:
                  pattern: o1*
                  capabilities:
                    supports-reasoning: true
                    supports-parallel-tool-calls: false
                    context-window: 200000
                    max-output-tokens: 100000
                o3:
                  pattern: o3*
                  capabilities:
                    supports-reasoning: true
                    context-window: 200000
                    max-output-tokens: 100000
                o4:
                  pattern: o4*
                  capabilities:
                    supports-reasoning: true
                    context-window: 200000
                    max-output-tokens: 100000
                gpt-4o:
                  pattern: gpt-4o*
                  capabilities:
                    input-modalities:
                      user-message: [text, image, audio]
                    max-output-tokens: 16384
                gpt-4-1:
                  pattern: gpt-4.1*
                  capabilities:
                    context-window: 1000000
                    max-output-tokens: 32768
                gpt-fallback:
                  pattern: gpt-*
                  capabilities: {}

            # Responses API: function call outputs accept text, image and file
            # content items per the SDK's FunctionCallOutput.Output union.
            openai-responses:
              defaults:
                input-modalities:
                  user-message: [text, image]
                  tool-result:  [text, image, pdf]
                output-modalities:
                  assistant-message: [text]
                supports-reasoning: false
                supports-reasoning-signature-roundtrip: false
                supports-prompt-caching: true
                supports-parallel-tool-calls: true
                context-window: 128000
                max-output-tokens: 4096
              models:
                gpt-5:
                  pattern: gpt-5*
                  capabilities:
                    supports-reasoning: true
                    supports-reasoning-signature-roundtrip: true
                    context-window: 400000
                    max-output-tokens: 128000
                o1:
                  pattern: o1*
                  capabilities:
                    supports-reasoning: true
                    supports-reasoning-signature-roundtrip: true
                    supports-parallel-tool-calls: false
                    context-window: 200000
                    max-output-tokens: 100000
                o3:
                  pattern: o3*
                  capabilities:
                    supports-reasoning: true
                    supports-reasoning-signature-roundtrip: true
                    context-window: 200000
                    max-output-tokens: 100000
                o4:
                  pattern: o4*
                  capabilities:
                    supports-reasoning: true
                    supports-reasoning-signature-roundtrip: true
                    context-window: 200000
                    max-output-tokens: 100000
                gpt-4o:
                  pattern: gpt-4o*
                  capabilities:
                    input-modalities:
                      user-message: [text, image, audio]
                    max-output-tokens: 16384
                gpt-4-1:
                  pattern: gpt-4.1*
                  capabilities:
                    context-window: 1000000
                    max-output-tokens: 32768
                gpt-fallback:
                  pattern: gpt-*
                  capabilities: {}
