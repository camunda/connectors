[
  {
    "testDescription": "Regular happy case with all secrets for Detailed connection",
    "database": "MYSQL",
    "connection": {
      "authType": "detailed",
      "host": "{{secrets.HOST_KEY}}",
      "port": "{{secrets.PORT_KEY}}",
      "username": "{{secrets.USERNAME_KEY}}",
      "password": "{{secrets.PASSWORD_KEY}}"
    },
    "data": {
      "returnResults": false,
      "query": "{{secrets.QUERY_KEY}}",
      "variables": "[{{secrets.VARIABLES_KEY}}]"
    }
  },
  {
    "testDescription": "Regular happy case with all secrets for URI connection",
    "database": "MYSQL",
    "connection": {
      "authType": "uri",
      "uri": "{{secrets.URI_KEY}}"
    },
    "data": {
      "returnResults": false,
      "query": "{{secrets.QUERY_KEY}}",
      "variables": "[{{secrets.VARIABLES_KEY}}]"
    }
  }
]