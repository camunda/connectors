{
  "properties": [
    {
      "name": "camunda.connector.configuration-validation.enabled",
      "type": "java.lang.Boolean",
      "description": "Whether credential validation (POST /configurations/validate) is served. The route presents resolved secrets to the endpoint the configuration names and the runtime ships no authentication, so restrict the path in front of the runtime before enabling it.",
      "defaultValue": false
    }
  ]
}
