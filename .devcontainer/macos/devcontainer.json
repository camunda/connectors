{
  "name": "connectors-macos",
  "build": {
    "context": "..",
    "dockerfile": "../Dockerfile"
  },

  "features": {
    "ghcr.io/devcontainers/features/java:1": {
      "version": "21",
      "installMaven": false
    },
    "ghcr.io/devcontainers/features/node:1": {
      "version": "24"
    },
    "ghcr.io/devcontainers/features/docker-outside-of-docker:1": {
      "moby": false
    }
  },

  "remoteUser": "dev",

  "mounts": [
    "source=/run/host-services/ssh-auth.sock,target=/ssh-agent,type=bind",
    "source=${localEnv:HOME}/.gitconfig,target=/home/dev/.gitconfig,type=bind,readonly",
    "source=connectors-maven-cache,target=/home/dev/.m2,type=volume",
    "source=connectors-npm-cache,target=/home/dev/.npm,type=volume"
  ],

  "remoteEnv": {
    "SSH_AUTH_SOCK": "/ssh-agent"
  },

  "containerEnv": {
    "TESTCONTAINERS_HOST_OVERRIDE": "host.docker.internal"
  },

  "postCreateCommand": "bash .devcontainer/post-create.sh",
  "postStartCommand": "sudo chmod 666 /ssh-agent"
}
