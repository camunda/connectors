# Pinned to the LocalStack community edition. This tag is pulled directly from Docker Hub with
# no internal mirror today; see docs/adr/ADR-0006-localstack-test-image-strategy.md for the
# EOL/licensing risk this creates and the long-term options being considered. To redirect this
# pull to a mirror without editing this file, set the CONNECTORS_TEST_IMAGE_REGISTRY environment
# variable (see DockerImages.java) — unset, resolution is unchanged.
localstack=localstack/localstack:4.8